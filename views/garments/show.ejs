<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Garment Detail</title>
</head>
<body>
    <h1><%= garment.name %></h1>
    <ul>
        <li>Location: <%= garment.location %></li>
        <li>Contact: <%= garment.contact %></li>
    </ul>
    <h2>List Products</h2>
    <ul>
        <% for(let product of garment.products) { %>
            <li>
                <a href="/products/<%= product._id %>"><%= product.name %> - <%= product.brand %></a></li>
        <% } %>
    </ul>

    <a href="/garments/<%= garment._id %>/edit">Edit</a>
    <a href="/garments/<%= garment._id %>/products/create">Add a Product</a>
    <form action="/garments/<%= garment._id %>?_method=DELETE" method="post">
        <button type="submit">Delete</button>
    </form>
    <a href="/garments">Back</a>
</body>
</html>